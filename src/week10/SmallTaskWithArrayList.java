package week10;

import day28ArrayList.ArraysListMethod1;
import ownCalisma.Array;

import java.util.ArrayList;
import java.util.Arrays;

public class SmallTaskWithArrayList {
    public static void main(String[] args) {
        /*
        Write a program that can set the last element of the Integer arrayList to zero
        ex; list[1,2,3,4,5]
        output=[1,2,3,4,0]
         */

        ArrayList<Integer>numbers = new ArrayList<>();
        numbers.addAll(Arrays.asList(1,2,3,4,5));

        System.out.println("numbers ="+ numbers);

        numbers.set(numbers.size()-1,0);
        System.out.println("number ="+numbers);

/*
2.Write a program that can swap first and last object from the arrayList.
 */

        //public static ArrayList<Integer> numberSwapper(ArrayList<Integer> arrayList){




// Birseyler yanlis oldu yineeeeeeee

// oscarin dersinden , kayitlatrdan bul lazim olursa


      /*  public static ArrayList<Integer> oddMultiplier(ArrayList<Integer> arrayList){
            // need to iterate through my arraylist to find and multiply our ODD numbers
            // we use % modulus operator
            for (int i = 0; i < arrayList.size(); i++) {
                if(arrayList.get(i)%2==1){ // checking ODD numbers
                    arrayList.set(i,arrayList.get(i)*2);
                }
            }
           // return arrayList;
        }



// en son yapilan codu coppy paste yaptim ama yinde arraysList ve arrayler error verdi b u yuzden commet aly=tina alamak zirunda akalidim

   */

    }







}
